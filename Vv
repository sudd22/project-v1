vmjjs 129999 dhdhh
sasdhjjygg
sadasda
ssdad
dsv
zcd
dynamodb
dynamo cache
gitactions
Built a CI/CD pipeline on AWS CodePipeline that pulls from GitHub, runs tests in CodeBuild, and deploys container images to ECS Fargate using blue-green deployment strategy for zero downtime releases.

Provisioned VPC networking with Terraform including public and private subnets across two availability zones, NAT gateways, security groups, and Route53 records for internal service discovery.

Configured Amazon EKS cluster with managed node groups, AWS Load Balancer Controller, and cluster autoscaler to scale Kubernetes workloads based on CPU and memory metrics during peak traffic.

Implemented infrastructure monitoring using CloudWatch dashboards, custom metrics from Lambda functions, SNS alerts for high error rates, and X-Ray tracing across distributed microservices architecture.

Set up AWS Secrets Manager integration with ECS task definitions and Parameter Store for environment-specific configuration, removing hardcoded credentials from application code and deployment manifests entirely.

Designed disaster recovery plan using cross-region S3 replication, RDS automated backups with point-in-time recovery, and documented runbooks for failover testing quarterly across production environments.

Automated SSL certificate management with ACM for CloudFront distributions and Application Load Balancers, enforcing HTTPS redirect policies and TLS 1.2 minimum across all customer-facing endpoints.

Created reusable Terraform modules for standardizing S3 buckets, IAM roles, and CloudWatch log groups across dev, staging, and production accounts using AWS Organizations and SCP guardrails.

Migrated legacy on-prem Jenkins jobs to GitHub Actions with OIDC authentication to AWS, eliminating long-lived access keys and enabling temporary role assumption for secure deployments.

Optimized ECS Fargate task sizing using Container Insights metrics, right-sizing CPU and memory allocation reduced monthly compute costs while maintaining p99 latency under two hundred milliseconds.

EKS node group autoscaling review.

Terraform state backend on S3.

ArgoCD sync policy notes.

